---
title: "Camel AI"
description: "Firecrawl se integra con Camel AI como cargador de datos."
og:title: "Camel AI | Firecrawl"
og:description: "Firecrawl se integra con Camel AI como cargador de datos."
---

<div id="installation">
  ## Instalación
</div>

```bash
pip install camel-ai

```

<div id="usage">
  ## Uso
</div>

Con Camel AI y Firecrawl puedes crear rápidamente sistemas multiagente que utilizan datos de la web.

<div id="using-firecrawl-to-gather-an-entire-website">
  ### Cómo usar Firecrawl para recopilar un sitio web completo
</div>

```python
mock_app = MockFirecrawlApp.return_value
firecrawl = Firecrawl(
    api_key='FC_API_KEY', api_url='https://api.test.com'
)
url = 'https://example.com'
response = [{'markdown': 'Contenido de Markdown'}]
mock_app.crawl_url.return_value = respons
result = firecrawl.markdown_crawl(url)
```

<div id="using-firecrawl-to-gather-a-single-page">
  ### Uso de Firecrawl para obtener una sola página
</div>

```python
mock_app = MockFirecrawlApp.return_value
firecrawl = Firecrawl(
    api_key='test_api_key', api_url='https://api.test.com'
)
url = 'https://example.com'
response = 'Contenido scrapeado'
mock_app.scrape_url.return_value = response

result = firecrawl.scrape(url)
```
