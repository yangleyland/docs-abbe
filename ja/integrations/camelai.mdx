---
title: "Camel AI"
description: "Firecrawl は Camel AI と連携し、データローダーとして機能します。"
og:title: "Camel AI | Firecrawl"
og:description: "Firecrawl は Camel AI と連携し、データローダーとして機能します。"
---

<div id="installation">
  ## インストール
</div>

```bash
pip install camel-ai

```

<div id="usage">
  ## 使用方法
</div>

Camel AI と Firecrawl を使うことで、ウェブ上のデータを活用するマルチエージェントシステムを迅速に構築できます。

<div id="using-firecrawl-to-gather-an-entire-website">
  ### Firecrawl でウェブサイト全体を収集する
</div>

```python
mock_app = MockFirecrawlApp.return_value
firecrawl = Firecrawl(
    api_key='FC_API_KEY', api_url='https://api.test.com'
)
url = 'https://example.com'
response = [{'markdown': 'Markdown コンテンツ'}]
mock_app.crawl_url.return_value = respons
result = firecrawl.markdown_crawl(url)
```

<div id="using-firecrawl-to-gather-a-single-page">
  ### Firecrawl を使って 1 ページを取得する
</div>

```python
mock_app = MockFirecrawlApp.return_value
firecrawl = Firecrawl(
    api_key='test_api_key', api_url='https://api.test.com'
)
url = 'https://example.com'
response = 'Scraped content'
mock_app.scrape_url.return_value = response

result = firecrawl.scrape(url)
```
