---
title: "Camel AI"
description: "O Firecrawl integra-se ao Camel AI como carregador de dados."
og:title: "Camel AI | Firecrawl"
og:description: "O Firecrawl integra-se ao Camel AI como carregador de dados."
---

<div id="installation">
  ## Instalação
</div>

```bash
pip install camel-ai

```

<div id="usage">
  ## Uso
</div>

Com Camel AI e Firecrawl, você pode criar rapidamente sistemas multiagentes que utilizam dados da web.

<div id="using-firecrawl-to-gather-an-entire-website">
  ### Usando o Firecrawl para Capturar um Site Inteiro
</div>

```python
mock_app = MockFirecrawlApp.return_value
firecrawl = Firecrawl(
    api_key='FC_API_KEY', api_url='https://api.test.com'
)
url = 'https://example.com'
response = [{'markdown': 'Conteúdo em Markdown'}]
mock_app.crawl_url.return_value = respons
result = firecrawl.markdown_crawl(url)
```

<div id="using-firecrawl-to-gather-a-single-page">
  ### Usando o Firecrawl para obter uma única página
</div>

```python
mock_app = MockFirecrawlApp.return_value
firecrawl = Firecrawl(
    api_key='test_api_key', api_url='https://api.test.com'
)
url = 'https://example.com'
response = 'Conteúdo raspado'
mock_app.scrape_url.return_value = response

result = firecrawl.scrape(url)
```
