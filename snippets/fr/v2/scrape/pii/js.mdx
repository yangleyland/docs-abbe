```javascript JavaScript
import { Firecrawl } from 'firecrawl';

const firecrawl = new Firecrawl({ apiKey: "fc-VOTRE_CLÉ_API" });

const doc = await firecrawl.scrape('https://example.com/contact', {
  redactPII: true,
});

console.log(doc.markdown);
```