```rust Rust
use firecrawl::FirecrawlApp;
use std::collections::HashMap;

#[tokio::main]
async fn main() {
    // APIキーで FirecrawlApp を初期化する
    let app = FirecrawlApp::new("fc-YOUR_API_KEY").expect("FirecrawlApp の初期化に失敗しました");

    // キャッシュが1時間以内（3600000ms）ならそれを使用
    // 新規スクレイプより最大5倍高速！
    let mut scrape_params = HashMap::new();
    scrape_params.insert("formats", vec!["markdown"]);
    scrape_params.insert("maxAge", 3600000); // 1時間（ミリ秒）

    let scrape_result = app
        .scrape_url("https://firecrawl.dev", Some(scrape_params))
        .await
        .expect("URLのスクレイプに失敗しました");

    println!("{}", scrape_result.markdown.unwrap_or_default());
}
```
