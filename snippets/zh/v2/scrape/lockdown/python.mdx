```python Python
from firecrawl import Firecrawl

firecrawl = Firecrawl(api_key="fc-YOUR_API_KEY")

# 仅返回之前缓存的结果，不发起任何出站请求。
# 如果 URL 不在缓存中，则返回 SCRAPE_LOCKDOWN_CACHE_MISS。
scrape_result = firecrawl.scrape(
    'https://firecrawl.dev',
    formats=['markdown'],
    lockdown=True,
)

print(scrape_result.markdown)
```