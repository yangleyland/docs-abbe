---
hidden: true
noindex: true
title: 'Extract'
description: '使用 LLM 从页面提取结构化数据'
og:title: "Extract | Firecrawl"
og:description: "使用 LLM 从页面提取结构化数据"
---

import ExtractCURL from "/snippets/zh/v1/extract/base/curl.mdx";
import ExtractPython from "/snippets/zh/v1/extract/base/python.mdx";
import ExtractNode from "/snippets/zh/v1/extract/base/js.mdx";
import ExtractOutput from "/snippets/zh/v1/extract/base/output.mdx";
import ExtractNoSchemaCURL from "/snippets/zh/v1/extract/no-schema/curl.mdx";
import ExtractNoSchemaOutput from "/snippets/zh/v1/extract/no-schema/output.mdx";
import CheckExtractJobCURL from "/snippets/zh/v1/extract/status/curl.mdx";
import ExtractStatusPending from "/snippets/zh/v1/extract/status/pending.mdx";
import ExtractStatusAsync from "/snippets/zh/v1/extract/async-response/async.mdx";
import ExtractStatusDone from "/snippets/zh/v1/extract/status/completed.mdx";

<div id="introducing-extract-beta">
  ## 介绍 /extract（测试版）
</div>

<Warning>
  [请点击此处查看全新的 /extract 公测文档 🔥](/zh/features/extract)
</Warning>

借助大型语言模型（LLM），从单个 URL、多个 URL，或整个网站提取结构化数据。全新的 `/extract` 端点可帮助你：

* 一次性从整站提取结构化数据
* 连接或构建需要网站结构化数据的数据增益/富化应用
* 开发需要从多个网站获取干净数据的 AI 应用

<div id="considerations">
  ## 注意事项
</div>

`/extract` 端点通过可自定义的 schema 提供灵活的数据提取。可以通过调优 prompt 进一步提升结果质量。该功能目前处于测试阶段，欢迎提交反馈。

<div id="extracting-data">
  ## 数据提取
</div>

<div id="extract-endpoint">
  ### /extract 端点
</div>

用于从整个网站提取结构化数据。

在指定 URL 时，可以在其后追加 `/*`，以从整个网站路径而非单个页面提取信息。

例如，`https://firecrawl.dev/*` 将尝试从 firecrawl.dev 域下的所有页面提取数据。`/*` 功能仍在测试中，如有任何问题，请发送邮件至 help@firecrawl.com 与我们联系。

<div id="usage">
  ### 使用
</div>

<CodeGroup>
  <ExtractPython />

  <ExtractNode />

  <ExtractCURL />
</CodeGroup>

有关参数的更多信息，请参见 [API 参考](https://docs.firecrawl.dev/api-reference/endpoint/extract)。

<div id="response-sdks">
  ### 响应（SDKs）
</div>

<ExtractOutput />

<div id="response-async-or-not-using-sdks">
  ### 响应（异步或未使用 SDK）
</div>

<ExtractStatusAsync />

<div id="checking-extract-status">
  ### 检查提取状态
</div>

你可以使用 `/extract/ID` 端点来查看提取作业的状态。

<Note>该端点仅适用于正在进行中的提取作业，或最近完成（24 小时内）的提取作业。</Note>

<CodeGroup>
  <CheckExtractJobCURL />
</CodeGroup>

<div id="pending-response">
  #### 待响应
</div>

提取任务可能处于以下状态之一：

* `completed`：提取任务已成功完成。
* `processing`：提取任务仍在进行中。
* `failed`：提取任务发生错误，未能完成。
* `cancelled`：提取任务已被用户取消。

<ExtractStatusPending />

<div id="completed-response">
  #### 已完成的结果
</div>

<ExtractStatusDone />

<div id="extracting-without-schema">
  ### 无需提供 schema 的提取
</div>

现在只需向端点传入一个 `prompt`，即可在不提供 schema 的情况下进行提取。LLM 会自行决定数据结构。

<CodeGroup>
  <ExtractNoSchemaCURL />
</CodeGroup>

<div id="improving-results-with-web-search-external-links">
  ### 通过网页搜索与外部链接改进结果
</div>

如果你想改进抽取效果，可以向该端点传入 `enableWebSearch` 参数。这样它会尝试从外部链接（超出所提供 URL 范围）获取数据。

<div id="billing">
  ### 计费
</div>

在 `/extract` 处于测试阶段期间，我们将对每个用于生成最终响应的已抓取 URL 收取 5 个积分，以防止滥用。此策略未来可能会调整。

> 你是需要 Firecrawl API 密钥的 AI 代理吗？请参见 [firecrawl.dev/agent-onboarding/SKILL.md](https://www.firecrawl.dev/agent-onboarding/SKILL.md) 了解自动化引导说明。